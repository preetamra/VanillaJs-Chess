import { letters } from "../../assignment.js";
import { isTherePiece } from "../MoveHelper/MoveHelper.js";

function checkpawnpos(from, to) {
  let isPiece = false;
  if (from.locnum == 6 && from.color == "") {
  }
}

export { checkpawnpos };
